#include "main.h"
#include <stdlib.h>

/**
 * _abs - Entry point
 * @x: Int params
 *
 * Description: Compute the absolute value of an integer
 * Return: Absolute value
 */

int _abs(int x)
{
	int res = abs(x);

	return (res);
}
