#include "main.h"
#include <stdlib.h>

/**
 * _isabs - Entry point
 * @x: Int params
 *
 * Description: Compute the absolute value of an integer
 * Return: Absolute value
 */

int _isabs(int x)
{
	int res = abs(x);

	return (res);
}
